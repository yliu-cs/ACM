#include <bits/stdc++.h>
using namespace std;

int main(int argc, char *argv[]) {
    string s; cin >> s;
    reverse(s.begin(), s.end());
    cout << s;
    reverse(s.begin(), s.end());
    cout << s;
    return 0;
}

