#include <bits/stdc++.h>
using namespace std;

int main(int argc, char *argv[]) {
    string Str;
    cin >> Str;
    if (Str.find("TAT") != string::npos) {
        printf("Yes\n");
    }
    else {
        printf("No\n");
    }
    return 0;
}

