#include <bits/stdc++.h>
using namespace std;

int main(int argc, char *argv[]) {
    int t;
    scanf("%d", &t);
    for (int Case = 1; Case <= t; ++Case) {
        long long n;
        scanf("%lld", &n);
        printf("%lld\n", n - 1);
    }
    return 0;
}

